# Base URL of the Qrivo backend REST API (versioned).
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1

# Public site URL (used for SEO metadata / canonical URLs and scan tracking links).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
